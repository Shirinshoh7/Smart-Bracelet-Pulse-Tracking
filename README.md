# Система мониторинга пульса Smart Bracelet (Web Dashboard + Telegram Bot)

## 💡 Описание проекта

Это **Full-Stack** система мониторинга здоровья, разработанная как инструмент для пациентов и врачей. Проект состоит из двух ключевых, работающих одновременно компонентов: Web Dashboard и Telegram-бота.

**Основной функционал:**

* **Автоматическое оповещение:** Если пульс пользователя поднимается выше критического порога (выше 100 уд/мин), **автоматически отправляется уведомление врачу** через Telegram-бот.
* **Мониторинг в реальном времени:** Врач может в любой момент зайти на **Web Dashboard** и посмотреть пульс пациента в режиме **реального времени**.

---

## 🛠️ Продемонстрированные навыки и Технологии

Этот проект демонстрирует владение ключевыми инструментами Python-разработки для позиции Junior Developer:

* **Backend и API:** Python, **Flask** (для создания Web-сервера), **JSON/REST API** (маршруты `/data` и `/average`).
* **Параллельная работа:** Использование **`threading`** (`run_all.py`) для одновременного запуска Web-сервера и Telegram-бота.
* **Асинхронное программирование:** Использование **`asyncio`** в логике Telegram-бота.
* **Web Frontend:** HTML/CSS, **JavaScript (Chart.js)** для построения динамического графика пульса в браузере.
* **Инструменты:** **Git/GitHub** (для контроля версий).

---

## 🚀 Инструкция по запуску

Для запуска проекта необходимо запустить оба компонента одновременно.

1.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```
2.  **Запустите проект:**
    *(Для запуска используется файл `run_all.py`, который запускает Web Backend и Telegram Bot в разных потоках)*
    ```bash
    python run_all.py
    ```

**Результат после запуска:**

* **Web Dashboard (для врача):** Доступен в браузере по адресу `http://127.0.0.1:5000/`.
* **Telegram Bot:** Начнет работу и будет готов к взаимодействию.
